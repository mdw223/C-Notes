// Declaration for variable x, so the compiler will know
// its type in any source file that include sthis header.
extern int x;

// Prototype for a funciton, f, so the compiler will know that
// it exists and what type it has.
void f( int );
